import { notFound } from "next/navigation";
import { requireUser } from "@/lib/auth/session";
import { db } from "@/lib/db";
import { formatPrice } from "@/utils/format";
import { orderStatusLabel, orderStatusTone } from "@/utils/status";
export default async function OrderDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;const user=await requireUser();const order=await db.order.findFirst({where:{id,userId:user.id},include:{items:true,payments:true}});if(!order)notFound();return <div><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs text-muted">جزئیات سفارش</p><h1 className="mt-1 text-2xl font-black" dir="ltr">{order.orderNumber}</h1></div><span className={`rounded-full px-3 py-2 text-xs font-bold ${orderStatusTone[order.status]}`}>{orderStatusLabel[order.status]}</span></div><div className="surface-card mt-6 divide-y divide-black/5">{order.items.map(item=><div key={item.id} className="flex justify-between gap-4 p-5"><div><b>{item.productName}</b><p className="mt-1 text-xs text-muted">{item.sku} · تعداد {new Intl.NumberFormat("fa-IR").format(item.quantity)}</p></div><b className="price-ltr text-sm">{formatPrice(item.total)}</b></div>)}</div><div className="mt-5 grid gap-4 sm:grid-cols-2"><section className="surface-card p-5"><h2 className="font-black">نشانی تحویل</h2><p className="mt-3 text-sm leading-7 text-muted">{order.recipientName}<br/>{order.province}، {order.city}، {order.addressLine}<br/>کد پستی: <span dir="ltr">{order.postalCode}</span></p></section><section className="surface-card p-5"><h2 className="font-black">خلاصه مالی</h2><dl className="mt-3 space-y-2 text-sm"><div className="flex justify-between"><dt>جمع کالاها</dt><dd>{formatPrice(order.subtotal)}</dd></div><div className="flex justify-between"><dt>تخفیف</dt><dd>{formatPrice(order.discount)}</dd></div><div className="flex justify-between border-t pt-2 font-black"><dt>مبلغ نهایی</dt><dd>{formatPrice(order.total)}</dd></div></dl></section></div></div>}
