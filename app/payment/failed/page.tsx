import Link from "next/link";
import { XCircle } from "lucide-react";
export default async function FailedPage({ searchParams }: { searchParams: Promise<Record<string,string|undefined>> }) { const params = await searchParams; return <div className="container-shell grid min-h-[65vh] place-items-center py-12 text-center"><div><XCircle className="mx-auto text-red-700" size={64} /><h1 className="mt-5 text-3xl font-black">پرداخت کامل نشد</h1><p className="mt-3 max-w-md leading-7 text-muted">برای سفارش <b dir="ltr">{params.order}</b> وجهی دریافت نشده است. می‌توانید سبد را بررسی و دوباره سفارش ثبت کنید.</p><div className="mt-7 flex justify-center gap-3"><Link href="/cart" className="btn-primary">بازگشت به سبد</Link><Link href="/contact" className="btn-secondary">درخواست راهنمایی</Link></div></div></div>; }
