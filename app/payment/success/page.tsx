import Link from "next/link";
import { CheckCircle2 } from "lucide-react";
export default async function SuccessPage({ searchParams }: { searchParams: Promise<Record<string,string|undefined>> }) { const params = await searchParams; return <div className="container-shell grid min-h-[65vh] place-items-center py-12 text-center"><div><CheckCircle2 className="mx-auto text-sage" size={64} /><h1 className="mt-5 text-3xl font-black">پرداخت با موفقیت ثبت شد</h1><p className="mt-3 text-muted">سفارش <b dir="ltr">{params.order}</b> وارد مرحله پردازش شد.</p>{params.ref && <p className="mt-2 text-xs text-muted">شماره پیگیری: <b dir="ltr">{params.ref}</b></p>}<div className="mt-7 flex justify-center gap-3"><Link href="/account/orders" className="btn-primary">مشاهده سفارش</Link><Link href="/shop" className="btn-secondary">ادامه خرید</Link></div></div></div>; }
