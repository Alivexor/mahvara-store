import { notFound } from "next/navigation";
import { AdminProductEditForm } from "@/components/admin/admin-product-edit-form";
import { requireAdmin } from "@/lib/auth/session";
import { db } from "@/lib/db";
export default async function EditProduct({params}:{params:Promise<{id:string}>}){await requireAdmin();const {id}=await params;const [product,categories,brands]=await Promise.all([db.product.findUnique({where:{id},include:{inventory:true,images:{orderBy:{sortOrder:"asc"},take:1}}}),db.category.findMany({where:{isActive:true},select:{id:true,name:true}}),db.brand.findMany({select:{id:true,name:true}})]);if(!product)notFound();return <div><p className="eyebrow">ویرایش محصول</p><h1 className="mt-2 text-2xl font-black">{product.name}</h1><AdminProductEditForm categories={categories} brands={brands} product={{id:product.id,name:product.name,slug:product.slug,sku:product.sku,shortDescription:product.shortDescription,description:product.description,productType:product.productType,price:product.price,salePrice:product.salePrice,categoryId:product.categoryId,brandId:product.brandId,stock:product.inventory?.stock??0,imageUrl:product.images[0]?.url??"/images/category-skincare.png"}}/></div>}
