"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function AdminProductForm({categories,brands}:{categories:{id:string;name:string}[];brands:{id:string;name:string}[]}){const [message,setMessage]=useState("");const [loading,setLoading]=useState(false);const router=useRouter();async function submit(formData:FormData){setLoading(true);setMessage("");const payload={name:formData.get("name"),slug:formData.get("slug"),sku:formData.get("sku"),shortDescription:formData.get("shortDescription"),description:formData.get("description"),productType:formData.get("productType"),price:Number(formData.get("price")),salePrice:formData.get("salePrice")?Number(formData.get("salePrice")):null,stock:Number(formData.get("stock")),lowStockThreshold:5,categoryId:formData.get("categoryId"),brandId:formData.get("brandId"),imageUrl:"/images/category-skincare.png",skinTypes:["همه انواع"]};const response=await fetch("/api/admin/products",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const data=await response.json() as {message?:string};setMessage(response.ok?"محصول با موفقیت ساخته شد.":data.message??"خطا در ثبت محصول");setLoading(false);if(response.ok)router.refresh()}return <form action={submit} className="surface-card p-5"><h2 className="text-lg font-black">محصول جدید</h2><div className="mt-5 grid gap-4 md:grid-cols-2"><div><label className="field-label" htmlFor="p-name">نام محصول</label><input className="field" id="p-name" name="name" required /></div><div><label className="field-label" htmlFor="p-slug">Slug انگلیسی</label><input className="field text-left" dir="ltr" id="p-slug" name="slug" pattern="[a-z0-9-]+" required /></div><div><label className="field-label" htmlFor="p-sku">SKU</label><input className="field text-left" dir="ltr" id="p-sku" name="sku" required /></div><div><label className="field-label" htmlFor="p-type">نوع محصول</label><input className="field" id="p-type" name="productType" required /></div><div><label className="field-label" htmlFor="p-category">دسته</label><select className="field" id="p-category" name="categoryId" required>{categories.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></div><div><label className="field-label" htmlFor="p-brand">برند</label><select className="field" id="p-brand" name="brandId" required>{brands.map(b=><option value={b.id} key={b.id}>{b.name}</option>)}</select></div><div><label className="field-label" htmlFor="p-price">قیمت (تومان)</label><input className="field" id="p-price" name="price" type="number" min="1" required /></div><div><label className="field-label" htmlFor="p-sale">قیمت تخفیف</label><input className="field" id="p-sale" name="salePrice" type="number" min="1" /></div><div><label className="field-label" htmlFor="p-stock">موجودی</label><input className="field" id="p-stock" name="stock" type="number" min="0" required /></div><div className="md:col-span-2"><label className="field-label" htmlFor="p-short">توضیح کوتاه</label><input className="field" id="p-short" name="shortDescription" minLength={10} required /></div><div className="md:col-span-2"><label className="field-label" htmlFor="p-desc">توضیحات کامل</label><textarea className="field min-h-28" id="p-desc" name="description" minLength={20} required /></div></div><div className="mt-4 flex items-center gap-3"><button className="btn-primary" disabled={loading}>{loading?"در حال ثبت…":"ساخت محصول"}</button><p className="text-sm font-bold text-brand" aria-live="polite">{message}</p></div></form>}
